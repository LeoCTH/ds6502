#pragma once

// memo to self:
// types with the prefix `ds6502_` are 6502 specific and
// the ones with `ds_` can be repurposed for other architectures
// i think
#include "ds6502/cpu.h"
#include "ds6502/memory.h"
#include "ds6502/error_checking.h"